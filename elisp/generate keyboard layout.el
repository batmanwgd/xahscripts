;; -*- coding: utf-8 -*-
;; 2012-05-27
;; generate a keyboard map in html


(defun generate-keymap (fPath)
  "generate the current keymap in a temp html file
and display it in brower."
  (let (
        titleStart
        tagStart

        )
    ;; (require 'sgml-mode)
    
    ))
